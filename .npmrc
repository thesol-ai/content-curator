# Hard-fail (not just warn) when the active Node is older than package.json
# "engines.node". The test suite's D1 integration harness imports node:sqlite,
# a Node 22.5+ builtin; without engine-strict an old Node fails later with a
# cryptic missing-module error instead of a clear up-front version message.
engine-strict=true
